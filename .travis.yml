os: osx
osx_image: xcode11.2

language: node_js
node_js: '16'

addons:
  chrome: stable

cache:
  yarn: true
  directories:
    - node_modules
    - $HOME/Library/Caches/electron
    - $HOME/Library/Caches/electron-builder

script:
  - yarn lint
  - travis_wait 90 yarn package
